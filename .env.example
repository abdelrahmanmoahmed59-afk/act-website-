# JWT secret for admin dashboard authentication (use a long random string).
JWT_SECRET=replace-with-long-random-string
JWT_ISSUER=act-admin

# Optional: allow a one-time bootstrap endpoint to create the first admin user.
ADMIN_BOOTSTRAP_SECRET=replace-with-long-random-string
